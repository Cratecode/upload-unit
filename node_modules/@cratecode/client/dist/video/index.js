"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.createVideo = exports.videoToAudio = void 0;
const main_1 = require("../proto_video/main");
/**
 * Extracts the audio file from a video file.
 * @param video {Buffer} - is the raw video data.
 */
function videoToAudio(video) {
    return Buffer.from(main_1.Main.fromBinary(new Uint8Array(video)).audio);
}
exports.videoToAudio = videoToAudio;
/**
 * Replaces the audio inside a video with a difference audio file.
 * @param initialVideo {Buffer} - is the initial video to use.
 * @param audio {Buffer} - is the new audio to put inside the video.
 */
function createVideo(initialVideo, audio) {
    const video = main_1.Main.fromBinary(new Uint8Array(initialVideo));
    video.audio = new Uint8Array(audio);
    return Buffer.from(main_1.Main.toBinary(video));
}
exports.createVideo = createVideo;
//# sourceMappingURL=index.js.map